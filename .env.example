# ========================================
# Rift Rewind - 环境变量配置示例
# ========================================
# 
# 使用说明：
# 1. 复制此文件为 .env.local (本地开发)
# 2. 在 AWS Amplify 控制台配置这些变量（生产环境）
# 3. 替换下面的值为你的实际 API 端点
#

# ========================================
# AWS API Gateway URLs
# ========================================

# 主数据 API (riftlens-read Lambda)
# 格式: https://xxxxx.execute-api.REGION.amazonaws.com/STAGE
NEXT_PUBLIC_API_GATEWAY_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod

# 聊天 API (riftlens-chat Lambda)
# 格式: https://xxxxx.execute-api.REGION.amazonaws.com/STAGE/chat
NEXT_PUBLIC_CHAT_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod/chat

# ========================================
# AWS 区域配置
# ========================================

# AWS 区域 (可选)
NEXT_PUBLIC_AWS_REGION=us-east-1

# ========================================
# 其他配置 (可选)
# ========================================

# 启用调试模式
# NEXT_PUBLIC_DEBUG_MODE=true

# Data Dragon 版本 (如果需要覆盖默认值)
# NEXT_PUBLIC_DD_VERSION=15.22.1

# ========================================
# 注意事项
# ========================================
#
# 1. 所有客户端使用的变量必须以 NEXT_PUBLIC_ 开头
# 2. 不要提交 .env.local 到 Git (已在 .gitignore)
# 3. 在 Amplify 控制台配置生产环境变量
# 4. 修改环境变量后需要重新构建
#
